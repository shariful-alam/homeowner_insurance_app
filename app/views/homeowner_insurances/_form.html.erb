<%= form_with(model: homeowner_insurance) do |form| %>
  <% if homeowner_insurance.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(homeowner_insurance.errors.count, "error") %> prohibited this homeowner_insurance from being saved:</h2>

      <ul>
        <% homeowner_insurance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
